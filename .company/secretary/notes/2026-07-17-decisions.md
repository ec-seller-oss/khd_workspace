# 2026-07-17 意思決定・作業ログ

## 入札ウォッチャー：GAS実行ログの確認とv2への更新
- 菊池さんの実行ログ（10:58）を確認：**動いたのは`setupTrigger`のみ**（「毎朝6時のトリガーを設定しました」）。巡回本体`run`は未実行のため、動作確認はまだ完了していない。
- 菊池さん指示「仕入売却の日時・販売先・想定利益もスプシに入れ、商売上の論点やリスクを巡回時にスプシで完結させて」を受けv2に改修：
  - **案件パイプライン**シートを自動生成：検知日/情報源/件名/URL/区分/ステータス/入札締切/現物確認日/最低売却価格/想定売却額(保守・中央)/諸費用/入札上限(自動数式)/入札額/契約金額(自動=×1.1)/想定利益(自動)/販売先候補/リスク・論点/次アクション/結果 の20列
  - 新着検知→スプシに行追加（入札上限=(保守売却-30万-諸費用)/1.1 等の数式込み）→LINE通知（スプシへのリンク付き）
  - **販売先マスタ**シートを自動生成：旧車王(045-476-1019/kaitori@currentmotor.co.jp)・フレックス・ドリーム・最強買取jp・向陽自販・ガリバー友人＋不動産/動産の枠。調査済み連絡先を初期投入
  - 初回runは既読登録のみ（通知洪水の防止）、`testFetch`関数で巡回可否だけをログ確認できるように
- ⚠️必要な追加セットアップ：スプシを新規作成し、そのIDを`CONFIG.SPREADSHEET_ID`に設定（コード再貼付が必要）。
- ステータス運用：新着→精査中→入札準備→入札済→落札/落選→売却済/撤退。仕入(入札)日時・売却日・販売先・実利益まで全てこのシートで完結させる。

## v2.1：スプシID埋め込み＋ランクル案件の自動投入（菊池さん「お前がやれ」対応）
- 菊池さん作成のスプシ`03_入札案件パイプライン`（1_UPTbcKOvq9xVmsQZR6KAu5hkrREkYnsnn4BU6Eo7q8・マイドライブ直下）をDrive APIで実在確認し、`CONFIG.SPREADSHEET_ID`に埋め込み済み。
- 初回実行時に**ランクル案件（公告148号）を1行目に自動投入**：締切9/7・現物確認8/20・最低価格15万・想定売却100万/150万・諸費用12万→入札上限≈52.7万が数式で自動算出。販売先候補・リスク・次アクションも初期値入り。
- LINEトークンはスクリプトプロパティ`LINE_NOTIFY_TOKEN`からのフォールバック読込に対応（コード貼り替えのたびにトークンを書き直す必要をなくせる）。
- **リモート環境の限界（正直な記録）**：GASエディタへの貼付・実行はGoogleアカウント本人しかできないため、最後の「貼って`run`を1回実行」だけは菊池さんの操作が必要。スプシへの直書きも現状のDrive MCPには書込ツールがないため、行の投入はGAS側（ensureSheets_/seedLandcruiser_）に担わせる設計にした。

## v2.2：testFetch実ログ（17:39）に基づくURL修正
- 実測結果：**正常4**（国税庁37件・裁判所BIT10件・岩手県20件・都主税局8件）／**404が4**（東北財務局・関東財務局・年金機構・NEXCO東）／**501が1**（岩手中部水道企業団）／**403が1**（JR東＝bot遮断）／**200だが0件が4**（KSI=JS描画、北上・花巻・紫波=トップページが浅い）。
- 修正内容：
  - 東北財務局→`/tohoku/b6_baikyaku/kouji.html`（公示中）＋`nyuusatsu_yotei.html`（予定＝先回り用）の2本に
  - 関東財務局→`/kantou/kanzai/mokuji_00001.htm`
  - 年金機構→`/chotatu/nyusatsu/index.html`（旧URLはスペル違い`chotatsu`が404の原因）
  - NEXCO東→`/bids/`
  - 北上市・花巻市→入札・契約ページ直リンクに変更
  - JR東は403（bot遮断）のため除外し、**国有財産売却情報サイト**（kokuyuzaisan.akiya-athome.jp・全国DB）を代替追加
  - 501対策：非200時に標準UAなしで1回リトライする`fetchSite_`を追加（水道企業団向け）
  - KSIはJS描画のため取得弱のまま残置（国税庁・自治体側で実質カバー）
- 次の確認：v2.2貼り替え→`testFetch`再実行でNG残数を確認→`run`初回実行（既読化＋ランクル案件自動投入）。

## v2.2実ログ（2回目）：13/15サイト正常化・run初回実行も成功
- testFetch再実行：**13/15が正常取得**（国税庁37/裁判所10/東北財務局(公示中)3・(予定)3/関東財務局10/年金機構2/北上3/花巻22/岩手県20/NEXCO東18/都主税局8）。残る2件：
  - 岩手中部水道企業団＝UAリトライ後も501継続（サーバ側WAFの可能性、自動化での解決を諦め手動確認に切替）
  - 国有財産売却情報サイト＝403（bot遮断）。JR東の代替として追加したが同じ壁、月1手動確認に切替
  - 紫波町・KSIは0件のまま（トップページが浅い/JS描画のため。北上・花巻・岩手県等の周辺サイトで実質カバー）
- `run`初回実行も成功：「初期化完了」ログ確認、スプシに**案件パイプライン**（ランクル案件1行目、数式評価済み）と**販売先マスタ**が実際に生成されたことをDrive経由で確認済み。
- **バグ発見・修正**：想定利益(自動)の列(P)が`O列(契約金額自動)>0`を条件にしていたが、O列の数式が返す空文字`""`はGoogle Sheetsの比較で「テキスト＞数値」扱いとなり常にTRUEになる罠があり、入札額未決定の段階で誤って**1,380,000円**（=中央値1,500,000−0−諸費用120,000）と表示される不具合があった。**条件をN列(入札額決定)>0の直接判定に修正**（v2.3）。
  - 影響：既存スプシのP2セルは誤った値のまま残っている。**手動修正が必要**：セルP2に`=IF(AND(K2>0,N2>0),K2-FLOOR(N2*1.1)-L2,"")`を貼り直せば直る（入札額を決めるまでは空欄が正しい表示）。
- LINE通知は「トークン未設定」でスキップされたが、スプシへの記帳・数式評価自体は正常動作。LINE Notifyのトークン取得・設定は別途必要（line_notify_gas.js側も同じプレースホルダのままで、実トークンは未取得の可能性）。

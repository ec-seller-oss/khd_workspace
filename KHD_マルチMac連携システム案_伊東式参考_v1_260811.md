# KHD マルチMac連携システム案 v1（伊東式を参考に）

作成: 2026-08-11 ｜ 参考: バイセル雑談LINE 伊東さんのギブ（お盆の複数台連携体制）
関連資料: `01_オペモデル_Mac3台リモート活用_260628.pptx`（6/28版の進化形として位置づけ）

---

## 0. 結論サマリ

- **伊東式の「リモートセッションで入る」は、Git連携とは別物。** 旧Macで今やっているGit連携（開始pull/終了push）はファイルの同期であり、セッション（会話・作業中の文脈）は運ばない。伊東さんがやっているのは常駐機で `claude remote-control` を立ち上げ、外から claude.ai/code やスマホアプリで**その動いているセッションに入る**方式。やり方自体は菊池家の2台構成でもそのまま使える。
- **セッションがMac間で連携しないのは、やはりアプリの仕様。** セッションは各マシンの `~/.claude/projects/` にローカル保存され、マシン間同期は存在しない（公式仕様）。だから「同期させる」のではなく、**伊東式=セッションを1台に集約して、他の端末は入るだけ**が正解。6/28デッキの「旧Mac母艦」構想と同じ発想で、伊東さんが実運用で裏付けてくれた形。
- **菊池家は2台なので、伊東さんの「3役分担」を「2台+クラウド」で再現する。** 伊東さんの mac mini（母艦）の役割は旧Macが継続、旧MacBookの「Claude in Chrome専任」の役割は**旧Macが時間割で兼任**し、あふれる重作業は **Claude Code on the web（クラウドセッション）** に逃がす。クラウドが実質の「3台目」。

---

## 1. 伊東式の要点（何が優れているか）

| 伊東さんの工夫 | 効果 |
|---|---|
| 全セッションを mac mini（常駐機）で挙動させる | MacBookを閉じても回線が切れても作業が途切れない |
| MacBookは出先からリモートセッションで入るだけ | テザリング等の弱い回線でも軽い（指示↔受け答えの往復のみ） |
| 旧MacBookを Claude in Chrome 専任に「外注」 | ブラウザ自動化（スクレイピング・定時巡回）を母艦から分離 |
| スクレイピング作業を時間で並べてバッティング回避 | 1台のリソースを取り合わない |
| Outlook/Gmail/Teams を定時巡回→ToDo抽出 | 受信箱の処理そのものをAIの定常業務にした |

---

## 2. 事実確認（今回の疑問への回答）

### Q1. 「旧MacはGit連携だけやった。リモートセッションで入るやり方は一緒なのか？」

**別物。** 整理すると3方式ある：

| 方式 | 処理が動く場所 | 特徴 |
|---|---|---|
| Git連携（現状の旧Mac） | 各マシンそれぞれ | ファイルは共有されるが、セッション（文脈）は運ばれない |
| **Remote Control（伊東式・本命）** | 常駐機（旧Mac） | 常駐機で `claude remote-control` → 外から claude.ai/code・スマホアプリ・QRで接続。コードもファイルも常駐機側。6/28デッキの本命と同じ |
| Claude Code on the web（クラウド） | Anthropicのクラウド | `claude --cloud` で開始。マシン不要・閉じても継続。ただしローカルファイルは触れない（GitHubリポジトリのclone作業のみ） |

つまり旧Macを伊東式の母艦にするには、Git連携に**追加で** Remote Control の常駐を立てる（手順は §5）。

### Q2. 「セッションがMac間で連携できないのはアプリの仕様？」

**仕様で確定。** セッションは `~/.claude/projects/<project>/<session-id>.jsonl` にローカル保存され、マシン間同期は存在しない。`--resume`/`--continue` も同一マシン内のみ。CLI・デスクトップ・Webはそれぞれ独立の履歴を持つ。**「セッション同期」を待つのではなく「セッション集約+リモート接続」で設計するのが現時点の正解**（伊東式はまさにこれ）。

---

## 3. システム案：菊池版（2台+ゆーしMac+iPhone+クラウド）

```
                 ┌──────────────────────────────┐
   出先          │        自宅・常駐            │        クラウド
┌─────────┐     │  ┌─────────────────────┐    │   ┌──────────────────┐
│ 新Mac    │────▶│  │ 旧Mac（M1 Air 8GB） │    │   │ Claude Code      │
│（打席機）│ 入る│  │  = 母艦・兼任機      │    │   │ on the web       │
└─────────┘     │  │ ① Remote Control常駐 │    │   │ = 実質の3台目     │
┌─────────┐     │  │   （03追客/01財務）  │◀───┼───│ 重作業の逃し先    │
│ iPhone   │────▶│  │ ② Claude in Chrome  │    │   │（git経由で成果回収）│
│          │ 入る│  │   定時巡回（時間割） │    │   └──────────────────┘
└─────────┘     │  └─────────────────────┘    │
                 └──────────────────────────────┘
   外部: ゆーしMac = ゆーし主・菊池のバックアップ実機（Git連携のみ推奨）
```

| 機器 | 役割 | 伊東式との対応 |
|---|---|---|
| **旧Mac**（自宅常設） | 母艦。①Remote Control常駐セッション（03追客/01財務）②Claude in Chrome 定時巡回 ③git母艦=書き手1台 | mac mini + 旧MacBook の2役を時間割で兼任 |
| **新Mac**（持ち出し） | 打席機。基本は claude.ai/code から旧Macのセッションに入るだけ（弱い回線でも軽い）。圏外時のみローカル起動→後でgit push | 伊東さんのMacBookと同じ |
| **iPhone** | Claudeアプリで同じセッションに入る。打席直後の1行投げ込み | 伊東さんのiPhoneと同じ |
| **クラウド（Claude Code on the web）** | 重い資料生成・時間のかかる一括処理を `claude --cloud` で投げる。旧Mac 8GBの天井の逃し先。成果はgit経由で回収 | mac mini の余力の代替（菊池家に3台目実機がない分をクラウドで補う） |
| **ゆーしMac**（外部） | 主はゆーし（EC折半）。菊池にとってはログイン済みのバックアップ実機。**常用はGit連携のみに留める**（第三者実機に巡回や常駐を置かない） | 伊東さんの「外部連携」に相当。ただし他人の実機なので役割は増やさない |

---

## 4. 時間割（伊東式バッティング回避・旧Mac 8GB前提）

旧Macは8GBなので、Chrome巡回とClaude Code重作業を**同時に走らせない**のが肝。

| 時間帯 | 旧Macの仕事 | 備考 |
|---|---|---|
| 5:00 | GAS: WBS未完了→LINE朝報（既存） | GAS側なので旧Mac負荷ゼロ |
| 5:30-6:30 | **Chrome巡回スロット①**: Gmail・カレンダー巡回→ToDo抽出→`.company/secretary/inbox/` へ | 朝の聖域(5-7)と並走OK。人は触らない時間 |
| 日中 | **Remote Control が主役**: 出先から入って転記・追客ドラフト・資料作成 | Chrome巡回は走らせない |
| 13:00前後 | **Chrome巡回スロット②**: 物件定時検索（レインズ等の定型チェックのみ） | 打席の谷間に固定 |
| 夜 | 重作業（査定・収支・報告書）。あふれる分はクラウドセッションへ | 就寝中に回して朝git pullで回収 |

- 巡回の出力先は既存の秘書室ルールに合流させる: 抽出ToDoは `.company/secretary/inbox/YYYY-MM-DD.md` に追記（同日1ファイル・上書き禁止）。
- 「自動提案の作り込み」はやらない（6/27羽鳥MEETの結論のまま）。Chrome巡回は**受信箱処理と定型チェックのみ**。

---

## 5. セットアップ手順（旧Macでやること）

1. **更新確認**: `claude --version` → 古ければアップデート
2. **常駐化**: tmux 内で `claude remote-control` を起動（server mode・複数接続対応）。tmuxに包んでおけばSSHやターミナル終了でも落ちない
   ```bash
   tmux new -s mothership
   cd ~/01_honbu_docs_automation && claude remote-control
   ```
   既存セッション中なら `/remote-control` でその場から有効化も可
3. **接続**: 表示されるQR/URLを iPhone のClaudeアプリまたは新Macの claude.ai/code で読む
4. **Claude in Chrome**: Chrome Web Store の Claude Code browser extension を導入 → `claude --chrome` で起動、`/chrome` から「Enabled by default」に。長時間運用で接続が切れたら `/chrome` で再接続
5. **寝かせない**: `caffeinate -di &` + システム設定でスリープOFF（電源OFFからの遠隔起動は不安定なので常時起動が手堅い）
6. **クラウド逃し**: 重作業は `claude --cloud "タスク内容"` で投げる。手元に引き取りたければ `claude --teleport`

---

## 6. 伊東式との違い（対比まとめ)

| 観点 | 伊東さん | 菊池版 |
|---|---|---|
| 母艦 | mac mini（常駐） | 旧Mac M1 Air 8GB（常駐） |
| Chrome専任機 | 旧MacBook（専任実機あり） | 旧Macが時間割で兼任 + 重作業はクラウドに逃がす |
| 知識共有 | Obsidian + Google Drive | git（workspace repo）+ Google Drive（現行のまま） |
| 外部連携 | あり | ゆーしMac（ただしGit連携のみに限定） |
| 巡回対象 | Outlook/Gmail/Teams + 物件検索 | Gmail/カレンダー + 物件定型チェック → 秘書室inboxへ |

## 7. リスク・注意

- **8GBの天井**: 常時2セッション+リモート配信+Chrome巡回はギリギリ。時間割厳守。恒常的に苦しければ「母艦を新Macに交代し旧MacをChrome専任化」（=伊東式に完全一致）が次の一手。ただし新Macが持ち出せなくなるため、まずはクラウド逃しで粘る。
- **ゆーしMacのセキュリティ**: 第三者実機に菊池のGoogle/Claude/顧客・財務データがログイン済みという状態は6/28時点からの継続課題。巡回や常駐など「無人で動く役割」は絶対に置かない。
- **Chrome巡回のコンテキスト消費**: Chromeツール常時ロードでコンテキストを食う。巡回セッションは母艦の作業セッションと分けて起動する。
